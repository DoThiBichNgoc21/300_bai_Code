#include <bits/stdc++.h>

using namespace std;

int main(){
    double a, b, c;
    cin >> a >> b >> c;
    double tbc = ( a + b + c ) / 3;
    cout << setprecision(1) << fixed << tbc;
    return 0;
}
